modules = ["nodejs-24", "python-3.11", "postgresql-16"]

# Production deploy is Fly.io Mumbai (see fly.toml + docs/COST_AND_DEPLOY.md).
# Do not use Replit Autoscale/GCE for Exotel Voicebot — the WS must stay up.

[workflows]
runButton = "Project"

[agent]
stack = "PNPM_WORKSPACE"
expertMode = true

[nix]
channel = "stable-25_05"
